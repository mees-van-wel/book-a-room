export interface SettingsInterface {
  id: string;
  companyName: string;
  email: string;
  phoneNumber: string;
  street: string;
  houseNumber: string;
  postalCode: string;
  city: string;
  kvkNumber: string;
  btwNumber: string;
  bicCode: string;
  iban: string;
}
