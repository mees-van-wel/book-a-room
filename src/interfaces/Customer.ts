export interface CustomerInterface {
  id: string;
  twCode: string;
  name: string;
  secondName: string;
  email: string;
  phoneNumber: string;
  street: string;
  houseNumber: string;
  postalCode: string;
  city: string;
  extra: string;
}
