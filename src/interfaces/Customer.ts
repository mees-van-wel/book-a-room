export interface CustomerInterface {
  id: string;
  name: string;
  secondName: string;
  email: string;
  phoneNumber: string;
  street: string;
  houseNumber: string;
  postalCode: string;
  city: string;
  priceOverride: number;
  extra: string;
}
