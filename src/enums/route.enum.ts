export const enum Route {
  Bookings = "/",
  Booking = "/bookings/[id]",
  Invoices = "/invoices",
  Invoice = "/invoices/[id]",
  Customers = "/customers",
  Rooms = "/rooms",
  CleaningSchedule = "/cleaning-schedule",
  Settings = "/settings",
  Auth = "/auth",
}
