export const enum Route {
  Bookings = "/",
  Booking = "/bookings/[id]",
  Invoices = "/invoices",
  Invoice = "/invoices/[id]",
  Customers = "/customers",
  Customer = "/customers/[id]",
  Rooms = "/rooms",
  Room = "/rooms/[id]",
  CleaningSchedule = "/cleaning-schedule",
  Settings = "/settings",
  Auth = "/auth",
}
